name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # كل 30 دقيقة — ضمن حد 2000 دقيقة/شهر
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -f "${{ secrets.RENDER_URL }}/health" || exit 0
